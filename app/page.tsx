export { default } from './contact/page';
```

Or simply **delete** `app/page.tsx` and **rename** your file from `app/contact/page.jsx` to `app/page.jsx`.

---

**Fix Option 2 — Visit the right URL**

If you want to keep it at `/contact`, just go to:
```
https://your-vercel-url.vercel.app/contact
